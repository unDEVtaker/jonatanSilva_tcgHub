<!DOCTYPE html>
<html>

<head>
    <title>Product Administration</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background-color: #000;
            color: #fff;
        }
        .table img {
            max-width: 50px;
            height: auto;
        }
    </style>
</head>

<body>
    <div class="container"> <%# Abre el container principal %>
        <header class="text-center my-4">
            <h1>Product Administration</h1>
        </header>

        <form class="mb-3" method="GET" action="/products/admin">
            <div class="input-group">
                <%# CORRECCIÓN AQUÍ: Accedemos a 'query.search' en lugar de 'req.query.search' %>
                <input type="text" class="form-control" name="search" placeholder="Search by Name, ID, Rarity, Price, Set, Card Number, Foil Type" value="<%= typeof query !== 'undefined' && typeof query.search !== 'undefined' ? query.search : '' %>">
                <button class="btn btn-outline-secondary" type="submit">Search</button>
            </div>
        </form>

        <%# Sección para mostrar el total (dentro del container principal) %>
        <%# Asegúrate de que 'toThousand' y 'totalValue' estén disponibles en la vista %>
        <div class="text-center my-4">
            <h3>Valor Total de tus Cartas Publicadas: $<%= toThousand(totalValue) %></h3>
        </div>
        <%# Fin Sección para mostrar el total %>


        <table class="table table-striped table-hover table-responsive table-dark">
            <thead>
                <tr>
                    <th class="text-center align-middle">Image</th>
                    <th class="text-center align-middle">ID</th>
                    <th class="text-center align-middle">Card ID</th>
                    <th class="text-center align-middle">Name</th>
                    <th class="text-center align-middle">Set</th>
                    <th class="text-center align-middle">Card Number</th>
                    <th class="text-center align-middle">Foil Type</th>
                    <th class="text-center align-middle">Rarity</th>
                    <th class="text-center align-middle">Price</th>
                    <th class="text-center align-middle">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% products.forEach(product=> { %>
                <tr>
                    <td class="text-center align-middle"><img src="<%= product.image %>"
                                alt="<%= product.name %>" width="50"></td>
                    <td class="text-center align-middle">
                        <%= product.id %>
                    </td>
                    <td class="text-center align-middle">
                        <%= product.cardId %>
                    </td>
                    <td class="text-center align-middle">
                        <%= product.name %>
                    </td>
                    <td class="text-center align-middle">
                        <%= product.set %>
                    </td>
                    <td class="text-center align-middle">
                        <%= product.cardNumber %>
                    </td>
                    <td class="text-center align-middle">
                        <%= product.foilType %>
                    </td>
                    <td class="text-center align-middle">
                        <%= product.rarity %>
                    </td>
                    <td class="text-center align-middle">
                        <%= toThousand(product.price ?? 0) %>
                    </td>
                    <td class="text-center align-middle">
                        <a href="/products/detail/<%= product.id %>" class="btn btn-success btn-sm me-1">
                            <i class="fas fa-info-circle"></i>
                        </a>
                        <a href="/products/edit/<%= product.id %>" class="btn btn-warning btn-sm me-1">
                            <i class="fas fa-edit"></i>
                        </a>
                        <a href="/products/delete/<%= product.id %>" class="btn btn-danger btn-sm"
                            onclick="return confirm('Delete this product?')">
                            <i class="fas fa-trash-alt"></i>
                        </a>
                    </td>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>

    </div> <%# Cierra el container principal %>


    <%# Sección para botones de acción debajo de la tabla (en un nuevo container) %>
    <div class="container my-3">
        <%# Usa un div para contener los botones y controlar su diseño %>
        <div class="d-flex justify-content-between">
            <%# Botón "Volver al Perfil" - Asume que 'user' está disponible en la vista (ej: via res.locals.user) %>
            <a href="/users/profile/<%= user.id %>" class="btn btn-secondary">Volver al Perfil</a> <%# Ruta al perfil del usuario logueado %>

            <%# Botón "Create New Product" (ya existía) %>
            <a href="/products/add" class="btn btn-primary">Create New Product</a>
        </div>
    </div>
    <%# Fin Sección para botones %>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
