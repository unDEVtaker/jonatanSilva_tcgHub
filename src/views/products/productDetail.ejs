<!DOCTYPE html>
<html>

<head>
  <title>Detalles del Producto</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    body {
      background-color: #000;
      color: #fff;
    }
  </style>
</head>

<body>
  <div class="container">
    <header class="text-center my-4">
      <h1>Product Details</h1>
    </header>

    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card bg-dark text-white">
          <div class="row g-0">
            <div class="col-md-4 d-flex align-items-center justify-content-center">
              <a href="<%= product.image %>" target="_blank">
                <img src="<%= product.image %>" class="img-fluid rounded-start m-2" alt="<%= product.name %>"
                  style="max-width: 150px;">
              </a>
            </div>
            <div class="col-md-8">
              <div class="card-body">
                <h5 class="card-title text-center">
                  <%= product.name %>
                </h5>
                <p class="card-text text-center"><strong>ID:</strong>
                  <%= product.id %>
                </p>
                <p class="card-text text-center"><strong>Card ID:</strong>
                  <%= product.cardId %>
                </p>
                <p class="card-text text-center"><strong>Set:</strong>
                  <%= product.set %>
                </p>
                <p class="card-text text-center"><strong>Card Number:</strong>
                  <%= product.cardNumber %>
                </p>
                <p class="card-text text-center"><strong>Foil Type:</strong>
                  <%= product.foilType %>
                </p>
                <p class="card-text text-center"><strong>Price:</strong> $<%= toThousand(product.price) %>
                </p>
                <p class="card-text text-center"><strong>Description:</strong>
                  <%= product.description %>
                </p>
                <div class="text-center">
                  <a href="/products/admin" class="btn btn-secondary me-2">Back</a>
                  <a href="/products/edit/<%= product.id %>" class="btn btn-warning me-2">Edit</a>
                  <a href="/products/delete/<%= product.id %>" class="btn btn-danger"
                    onclick="return confirm('Delete this product?')">Delete</a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>