<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <link rel='stylesheet' href='/stylesheets/header.css' />
  <link rel='stylesheet' href='/stylesheets/login.css' />
</head>

<body>
  <%- include('../partials/header') %>

    <div class="container-login">
      <div class="login-box">
        <div class="login-header">
          <header>Crear nueva cuenta</header>
        </div>

        <form action="/users/signup" method="POST">
          <div class="input-box">
            <input type="text" class="input-field" placeholder="Name" name="nombre" autocomplete="off" minlength="2" required>
            <% if(locals.errores && locals.errores.nombre) { %>
              <div class="text-danger">
                <%= errores.nombre.msg %>
              </div>
            <% } %>
          </div>
          <div class="input-box">
            <input type="text" class="input-field" placeholder="Email" name="correo" autocomplete="off" required>
            <% if(locals.errores && locals.errores.correo) { %>
              <div class="text-danger">
                <%= errores.correo.msg %>
              </div>
            <% } %>
          </div>
          <div class="input-box">
            <input type="password" class="input-field" placeholder="Password" name="contrasena" autocomplete="off"
              required>
            <% if(locals.errores && locals.errores.contrasena) { %>
              <div class="text-danger">
                <%= errores.contrasena.msg %>
              </div>
            <% } %>
          </div>
          <div class="input-submit">
            <button type="submit" class="submit-btn" id="submit">Crear Cuenta</button>
          </div>
          <div class="sign-up-link">
            <p>¿Ya tienes una cuenta?<a href="/users/login">Iniciar Sesión</a></p>
          </div>
        </form>
      </div>
    </div>

    <script src="/javascripts/script.js"></script>
</body>

</html>